# === Local (SQLite file) ===
# DATABASE_URL="file:./dev.db"

# === Producción (Turso / libSQL) ===
# Crea la BD: `turso db create roland-garrulos`
# Saca URL:   `turso db show roland-garrulos --url`
# Saca token: `turso db tokens create roland-garrulos`
DATABASE_URL="libsql://tu-db.turso.io"
DATABASE_AUTH_TOKEN="eyJ..."

# Contraseña del panel admin (cámbiala antes de publicar)
ADMIN_PASSWORD="admin123"
